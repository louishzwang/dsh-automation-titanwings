import type { AutomationViewProps } from './contracts.js';
/** Native conversation view: all data and effects arrive through the slot's four shares. */
export declare function AutomationView({ t, useAutomationState, refresh, createAutomation, mutateAutomation, runNow, markRunRead, openSession, }: AutomationViewProps): JSX.Element;
